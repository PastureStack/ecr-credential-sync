#!/bin/bash
set -euo pipefail

cd $(dirname $0)/..

echo Running tests

PACKAGES=$(go list ./... | grep -Ev '(/vendor/|/mocks$)' || true)
RACE=${RACE:-}

[ "${ARCH}" == "amd64" ] && RACE=-race
go test ${RACE} -cover -tags=test ${PACKAGES}
