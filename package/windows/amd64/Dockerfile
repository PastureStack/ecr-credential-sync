# escape=`
FROM mcr.microsoft.com/windows/servercore:ltsc2022

SHELL ["cmd", "/S", "/C"]

RUN echo The Windows ECR credentials image build path is intentionally disabled until a Windows host compatibility matrix is restored. && exit /b 1
